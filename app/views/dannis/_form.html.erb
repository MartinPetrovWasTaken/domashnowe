<%= form_with(model: danni, local: true) do |form| %>
  <% if danni.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(danni.errors.count, "error") %> prohibited this danni from being saved:</h2>

      <ul>
      <% danni.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nomer %>
    <%= form.text_field :nomer, id: :danni_nomer %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :danni_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
